{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.10"
    },
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install -r requirements.txt",
    "startCommand": "cd backend && python app.py"
  }
}
